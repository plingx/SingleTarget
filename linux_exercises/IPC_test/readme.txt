

ls -l | wc -l

一个父进程，执行ls -l的命令

ls -l 其结果，原本应输出到屏幕（fd[1]为屏幕的描述符）

现在将其结果，给到管道

子进程，读管道的结果，并执行wc -l，并将此结果显示到屏幕


为真，说明子进程正常终止。

为真，说明子进程是被信号终止的。

// 如果子进程未终止，父进程阻塞在此。

不关心子进程结束原因。

回收任意子进程

回收多个子进程


使用阻塞方式回收子进程

使用非阻塞方式回收子进程


